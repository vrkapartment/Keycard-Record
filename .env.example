# Supabase Postgres — pooled connection (used by the running app)
DATABASE_URL=

# Supabase Postgres — direct connection (used by Prisma CLI/migrations)
DIRECT_URL=

# Random signing key for session cookies, e.g. `openssl rand -base64 32`
SESSION_SECRET=
